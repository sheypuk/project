
let initialState = {
    dialogs: [
        {id: 1, name: 'Dimych'},
        {id: 2, name: 'Alena'},
        {id: 3, name: 'Oleg'},

    ],

};

 const sideReducer = (state = initialState, action) =>
{
    

    return state;
}

export default sideReducer;